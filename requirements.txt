# 核心数据处理
pandas>=2.0.0
numpy>=1.24.0

# 地理空间数据处理（矢量：Shapefile）
geopandas>=0.13.0

# 地理空间数据处理（栅格：GeoTIFF）
rasterio>=1.3.0
rioxarray>=0.15.0  # 可选，但推荐用于与xarray集成

# 数据可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# 统计分析
scipy>=1.10.0
statsmodels>=0.14.0  # 可选，用于趋势分析（如Mann-Kendall）

# 读取 Excel 文件
openpyxl>=3.1.0

# 时间处理（增强）
python-dateutil>=2.8.0

# 可选：交互式地图（用于探索性分析）
folium>=0.14.0
contextily>=1.3.0  # 用于添加底图

# 可选：更强大的栅格/多维数据处理
xarray>=2023.0